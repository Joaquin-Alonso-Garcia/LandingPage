<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Landing page</title>
	</head>
	<body class="text-black">
		<slot />
	</body>
</html>

<style>
	@import url('https://fonts.googleapis.com/css2?family=Playwrite+IN:wght@100..400&display=swap');
	body {
		position: relative;
		z-index: 1; /* Ensure the body content stays above the pseudo-elements */
	}

	/* Gradient Layer (bottom layer) */
	body::after {
		content: '';
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(to right, #ff8177 0%, #ff867a 0%, #ff8c7f 21%, #f99185 52%, #cf556c 78%, #b12a5b 100%);
		z-index: -2; /* Place the gradient below the image */
	}

	/* Image Layer (middle layer) */
	body::before {
		content: '';
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: url('../assets/brain-svg.svg') no-repeat center center;
		background-size: 400px 400px;
		background-position: center;
		opacity: 0.5; /* Adjust the opacity of the image */
		z-index: -1; /* Place the image above the gradient but below the content */
	}
</style>